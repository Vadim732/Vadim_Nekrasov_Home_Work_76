@model Establishment

@{
    ViewBag.Title = "Details";
}

<div class="container">
    <div class="row">
        <div class="col-6">
            <h1>@Model.Name</h1>
            <p>@Model.Description</p>
        </div>
        <div class="col-6">
            <img src="@Model.Image" class="img-fluid" style="height: 200px"/>
        </div>
    </div>
</div>